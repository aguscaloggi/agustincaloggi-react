import React from "react";

function ItemDetail ({item}) {
    const [ItemDetail, setItemDetail] = useState([
        useEffect(( => {
            
        }

        ))
    ]);
    return {
        
    }
}